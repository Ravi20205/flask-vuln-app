# Flask Vuln App

This is a deliberately vulnerable Flask app created for testing Software Composition Analysis (SCA) tools like Snyk, DeepSource, or Qwiet AI.

## Usage

Install dependencies:
```bash
pip install -r requirements.txt
